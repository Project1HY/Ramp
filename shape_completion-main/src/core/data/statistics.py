# mats = ds.full_shape_distance_mat((np.mean, np.max))
# print(f'Max(Mean)={np.max(mats[0])},Max(Max)={np.max(mats[1])},Mean(Max)={np.mean(mats[1])}')
# sns.heatmap(mats[0], linewidth=0.5, square=True)
# plt.title('Mean Coordinate difference')
# plt.show()
#
# sns.heatmap(mats[1], linewidth=0.5, square=True)
# plt.title('Max Coordinate difference')
# plt.show()